<div class="cube">
    <img class="cube" src="{{imageURL}}">
    <p class="name">{{name}}</p>
    <p><span>Difficulty level:</span> {{difLevel}}</p>
    <a class="btn" href="/cube/details/{{_id}}">Details</a>
    <a class="btn" href="/cube/{{_id}}/attach-accessory">Attach</a>
</div>